{
  "schemaVersion": 1,
  "description": "Per-job wall-clock budgets (minutes) for release.yml. A breach is written to the run summary and receipt and opens an incident issue; it never retries or hides the stage. The update-from-published-versions matrix runs in parallel after signing (old baselines build their runtime on first launch, 4-15 min per cell); totals include it.",
  "jobs": {
    "Select reusable installed acceptance evidence": 4,
    "Accept coordinated installers before signing": 30,
    "Sign and stage the exact product release": 7,
    "Sign the loopback update baseline": 7,
    "Signed native update and restart": 12,
    "Signed legacy Profile migration": 10,
    "Install signed bytes on a clean worker": 8,
    "Verify the signed feed with published client verifiers": 5,
    "Publish the accepted signed release and verify delivery": 5,
    "Remove this run's never-published drafts": 2
  },
  "queueMinutes": 3,
  "totalMinutes": {
    "hotfix": 35,
    "standard": 45
  }
}
