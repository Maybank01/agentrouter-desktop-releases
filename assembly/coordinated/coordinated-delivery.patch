diff --git a/apps/desktop-host/src/index.ts b/apps/desktop-host/src/index.ts
index e85badb..f06f24a 100644
--- a/apps/desktop-host/src/index.ts
+++ b/apps/desktop-host/src/index.ts
@@ -53,10 +53,17 @@ export interface DesktopHostFetchCommand {
 /** Commands accepted by the desktop child process. */
 export type DesktopHostCommand = {
   readonly type: 'shutdown'
+} | {
+  readonly type: 'prepare-restart' | 'cancel-restart'
+  readonly requestId: number
 }
 
 /** Events emitted by the desktop child process. */
 export type DesktopHostEvent = {
+  readonly type: 'restart-prepared'
+  readonly requestId: number
+  readonly idle: boolean
+} | {
   readonly type: 'ready'
   readonly protocolVersion: typeof DESKTOP_HOST_PROTOCOL_VERSION
   readonly dshVersion: string
@@ -67,6 +74,8 @@ export type DesktopHostEvent = {
 
 /** Controller returned to tests and the self-executing process entry. */
 export interface DesktopHostController {
+  prepareRestart(): Promise<boolean>
+  cancelRestart(): void
   /** Installed dsh version carried by this host. */
   readonly dshVersion: string
   /** Dispatch one custom-protocol request and stream its response to the response pipe. */
@@ -82,8 +91,9 @@ function isRecord(value: unknown): value is Record<string, unknown> {
 }
 
 function isDesktopHostCommand(message: unknown): message is DesktopHostCommand {
-  return typeof message === 'object' && message !== null && 'type' in message
-    && (message as Record<string, unknown>).type === 'shutdown'
+  if (!isRecord(message)) return false
+  return message.type === 'shutdown' || (['prepare-restart', 'cancel-restart'].includes(String(message.type))
+    && Number.isSafeInteger(message.requestId) && Number(message.requestId) > 0)
 }
 
 interface PackageManifest {
@@ -286,10 +296,20 @@ export async function runDesktopHost(
   writeFileSync(rootConfig, ROOT_CONFIG)
   const environment = loadLayeredEnv('dsh desktop')
   let current: Context | undefined
-  const ctx = await boot('dsh desktop', rootConfig, structuredClone(desktopPatches(
+  const patches = structuredClone(desktopPatches(
     absoluteProject,
     options.allowLinkedPackages === true,
-  )), (hostCtx) => {
+  ))
+  const releasePath = join(absoluteProject, 'desktop-release.json')
+  if (existsSync(releasePath)) {
+    const release = JSON.parse(readFileSync(releasePath, 'utf8'))
+    const manifest = JSON.parse(readFileSync(join(absoluteProject, 'package.json'), 'utf8'))
+    if (release.productVersion && manifest.dsh?.profile?.bundles?.includes('@agentrouter-top/dsh-codex')
+      && release.managedPlugins?.some((plugin: { name: string }) => plugin.name === '@agentrouter-top/dsh-codex')) {
+      patches.push({ id: 'agentrouter-plugin-updates', config: { owner: 'product' } })
+    }
+  }
+  const ctx = await boot('dsh desktop', rootConfig, patches, (hostCtx) => {
     current = hostCtx
     hostCtx.provide(DSH_LAUNCH_ENVIRONMENT_KEY, environment)
     provideCmdline(hostCtx, { args: [], exit: () => {} })
@@ -307,9 +327,18 @@ export async function runDesktopHost(
   const streams = remoteStreamHandler(ctx)
   const requests = new Map<number, AbortController>()
   let disposing: Promise<void> | undefined
+  let releaseMaintenance: (() => void) | undefined
+  let holding: Promise<void> | undefined
+  const cancelRestart = (): void => { releaseMaintenance?.(); releaseMaintenance = undefined; holding = undefined }
+  const agents = ctx.get('agents')
+  const lockAgent = (agent: { runMaintenance(task: () => Promise<void>): Promise<void> }): void => {
+    void agent.runMaintenance(() => holding!).catch(() => {})
+  }
+  ctx.on('agent/created', ({ agent }) => { if (holding) lockAgent(agent) })
 
   const dispose = async (): Promise<void> => {
     disposing ??= (async () => {
+      cancelRestart()
       for (const controller of requests.values()) controller.abort()
       requests.clear()
       await current?.fiber.dispose()
@@ -319,12 +348,21 @@ export async function runDesktopHost(
   }
 
   return {
+    async prepareRestart() {
+      if (holding) return true
+      if (agents === undefined || agents.list().some(agent => agent.status !== 'idle')) return false
+      holding = new Promise<void>(resolve => { releaseMaintenance = resolve })
+      try { for (const agent of agents.list()) lockAgent(agent) }
+      catch { cancelRestart(); return false }
+      return true
+    },
+    cancelRestart,
     dshVersion: dshVersion(absoluteProject),
     cancel(streamId) {
       requests.get(streamId)?.abort()
     },
     async fetch(command, body) {
-      if (disposing !== undefined) throw new Error('dsh desktop: host is disposing')
+      if (disposing !== undefined || holding !== undefined) throw new Error('dsh desktop: host is preparing to restart')
       const controller = new AbortController()
       requests.set(command.streamId, controller)
       try {
@@ -564,12 +602,22 @@ async function main(): Promise<void> {
   })
   requestPipe.once('error', failTransport)
   responsePipe.once('error', failTransport)
+  let restartRequest: number | undefined
   process.on('message', (message: unknown) => {
     if (!isDesktopHostCommand(message)) {
       send({ type: 'fatal', message: 'dsh desktop: invalid Electron IPC command' })
       void stop(1)
       return
     }
+    if (message.type === 'prepare-restart') {
+      restartRequest = message.requestId
+      void controller.prepareRestart().then(idle => send({ type: 'restart-prepared', requestId: message.requestId, idle }))
+      return
+    }
+    if (message.type === 'cancel-restart') {
+      if (restartRequest === message.requestId) controller.cancelRestart()
+      return
+    }
     void stop()
   })
   process.once('disconnect', () => { void stop() })
diff --git a/apps/desktop/electron-builder.config.mjs b/apps/desktop/electron-builder.config.mjs
index c47bd6d..9659de4 100644
--- a/apps/desktop/electron-builder.config.mjs
+++ b/apps/desktop/electron-builder.config.mjs
@@ -11,6 +11,7 @@ import {
 } from './scripts/windows-sign.mjs'
 import { resolveDesktopAutoUpdateConfig } from './scripts/desktop-auto-update-environment.mjs'
 import { desktopTargetBuildPaths } from './scripts/desktop-build-paths.mjs'
+import { existsSync, readFileSync } from 'node:fs'
 
 /**
  * Create electron-builder configuration from one release environment.
@@ -43,12 +44,18 @@ export function createElectronBuilderConfig(
   if (windowsSigner !== undefined) {
     installWindowsNsisBootstrapSigner({ sign: windowsSigner })
   }
-  const update = resolveDesktopAutoUpdateConfig(env, resolvedPlatform, resolvedArch)
+  const productFile = new URL('./product-release.json', import.meta.url)
+  const product = existsSync(productFile) ? JSON.parse(readFileSync(productFile, 'utf8')) : undefined
+  const update = product ? { target: resolvedPlatform === 'win32' ? 'win-x64' : `mac-${resolvedArch}`, publicUrl: product.updateUrl }
+    : resolveDesktopAutoUpdateConfig(env, resolvedPlatform, resolvedArch)
+  if (product && (typeof update.publicUrl !== 'string' || !update.publicUrl.startsWith('https://'))) {
+    throw new Error('product release requires an explicit HTTPS updater feed')
+  }
   const buildPaths = desktopTargetBuildPaths(update.target)
   return {
     appId,
-    productName: 'DeepSeek Harness',
-    artifactName: 'deepseek-harness-${version}-${os}-${arch}.${ext}',
+    productName: product ? 'AgentRouter' : 'DeepSeek Harness',
+    artifactName: product ? 'AgentRouter-${version}-${arch}-Setup.${ext}' : 'deepseek-harness-${version}-${os}-${arch}.${ext}',
     directories: { output: buildPaths.artifacts },
     asar: true,
     files: [
diff --git a/apps/desktop/renderer/plugin-manager.js b/apps/desktop/renderer/plugin-manager.js
index a50de3a..6aaad3d 100644
--- a/apps/desktop/renderer/plugin-manager.js
+++ b/apps/desktop/renderer/plugin-manager.js
@@ -52,7 +52,8 @@ async function main() {
       })
       const actions = document.createElement('span')
       actions.className = 'package-actions'
-      actions.append(update, remove)
+      if (plugin.updateOwner !== 'product') actions.append(update)
+      actions.append(remove)
       item.append(identity, actions)
       return item
     }))
diff --git a/apps/desktop/scripts/prepare-seed.ts b/apps/desktop/scripts/prepare-seed.ts
index 73da078..8d305ed 100644
--- a/apps/desktop/scripts/prepare-seed.ts
+++ b/apps/desktop/scripts/prepare-seed.ts
@@ -51,13 +51,16 @@ function manifestVersion(path: string, subject: string): string {
 function desktopRelease(): DesktopRelease {
   const version = manifestVersion(join(APP_ROOT, 'package.json'), 'desktop package')
   const dshVersion = manifestVersion(resolve(APP_ROOT, '..', '..', 'package.json'), 'root dsh package')
-  if (version !== dshVersion) {
+  const productPath = join(APP_ROOT, 'product-release.json')
+  const product = existsSync(productPath) ? JSON.parse(readFileSync(productPath, 'utf8')) : undefined
+  if (product === undefined && version !== dshVersion) {
     throw new Error(`desktop seed: Electron ${version} must bind the same version of @deepseek-ai/dsh, found ${dshVersion}`)
   }
   const runtime = JSON.parse(readFileSync(join(RUNTIME_ROOT, 'versions.json'), 'utf8')) as Record<string, unknown>
   return parseDesktopRelease({
     schemaVersion: 1,
-    version,
+    version: dshVersion,
+    ...(product === undefined ? {} : { productVersion: version, managedPlugins: product.managedPlugins }),
     hostProtocolVersion: DESKTOP_HOST_PROTOCOL_VERSION,
     nodeVersion: runtime.node,
     pnpmVersion: runtime.pnpm,
diff --git a/apps/desktop/src/host-process.ts b/apps/desktop/src/host-process.ts
index ed18d0c..f65c89f 100644
--- a/apps/desktop/src/host-process.ts
+++ b/apps/desktop/src/host-process.ts
@@ -33,6 +33,8 @@ function isDesktopHostEvent(message: unknown): message is DesktopHostEvent {
   if (typeof message !== 'object' || message === null || !('type' in message)) return false
   const candidate = message as Record<string, unknown>
   switch (candidate.type) {
+    case 'restart-prepared':
+      return Number.isSafeInteger(candidate.requestId) && typeof candidate.idle === 'boolean'
     case 'ready':
       return candidate.protocolVersion === DESKTOP_HOST_PROTOCOL_VERSION && typeof candidate.dshVersion === 'string'
     case 'fatal':
@@ -67,6 +69,31 @@ export interface DesktopHostReady {
 
 /** One dsh backend running under the bundled upstream Node.js executable. */
 export class DesktopHostProcess {
+  private restartId = 0
+  private restartResolve: ((idle: boolean) => void) | undefined
+  private restartReject: ((error: Error) => void) | undefined
+
+  /** Quiesce idle agents before a product update; never cancel running work. */
+  async prepareRestart(): Promise<boolean> {
+    if (this.restartResolve) throw new Error('desktop restart preparation already pending')
+    const requestId = ++this.restartId
+    let timer: ReturnType<typeof setTimeout> | undefined
+    try {
+      return await new Promise<boolean>((resolve, reject) => {
+        this.restartResolve = resolve
+        this.restartReject = reject
+        timer = setTimeout(() => reject(new Error('desktop restart preparation timed out')), 10000)
+        this.send({ type: 'prepare-restart', requestId })
+      })
+    } catch (error) {
+      try { this.send({ type: 'cancel-restart', requestId }) } catch { /* child has exited */ }
+      throw error
+    } finally {
+      clearTimeout(timer)
+      this.restartResolve = undefined
+      this.restartReject = undefined
+    }
+  }
   private child: ChildProcess | undefined
   private requestPipe: Writable | undefined
   private responsePipe: Readable | undefined
@@ -387,6 +414,9 @@ export class DesktopHostProcess {
 
   private handleMessage(message: DesktopHostEvent): void {
     switch (message.type) {
+      case 'restart-prepared':
+        if (message.requestId === this.restartId) this.restartResolve?.(message.idle)
+        return
       case 'ready':
         this.readyResolve(message)
         return
@@ -399,6 +429,7 @@ export class DesktopHostProcess {
   }
 
   private fail(error: Error): void {
+    this.restartReject?.(error)
     this.readyReject(error)
     for (const pending of this.pending.values()) {
       void pending.requestReader?.cancel(error).catch(() => undefined)
diff --git a/apps/desktop/src/host-protocol.ts b/apps/desktop/src/host-protocol.ts
index d057d6d..970a2b5 100644
--- a/apps/desktop/src/host-protocol.ts
+++ b/apps/desktop/src/host-protocol.ts
@@ -42,10 +42,17 @@ export interface DesktopHostRequestStart {
 /** Commands retained on Node IPC because they do not carry Fetch payload bytes. */
 export type DesktopHostCommand = {
   readonly type: 'shutdown'
+} | {
+  readonly type: 'prepare-restart' | 'cancel-restart'
+  readonly requestId: number
 }
 
 /** Lifecycle events retained on Node IPC. */
 export type DesktopHostEvent = {
+  readonly type: 'restart-prepared'
+  readonly requestId: number
+  readonly idle: boolean
+} | {
   readonly type: 'ready'
   readonly protocolVersion: typeof DESKTOP_HOST_PROTOCOL_VERSION
   readonly dshVersion: string
diff --git a/apps/desktop/src/main.ts b/apps/desktop/src/main.ts
index 8bc9fdc..5b8b832 100644
--- a/apps/desktop/src/main.ts
+++ b/apps/desktop/src/main.ts
@@ -19,6 +19,8 @@ import { DESKTOP_IPC, type DesktopUpdateState } from './ipc.ts'
 import { formatDesktopMessage, resolveDesktopLocale } from './locale.ts'
 import { claimDesktopSingleInstance } from './single-instance.ts'
 import { DesktopUpdateCoordinator } from './update-coordinator.ts'
+import { createProductUpdateRoutes } from './agentrouter-update-routes.mjs'
+import updateHistory from './agentrouter-release-history.json'
 
 const SCHEME = 'dsh-app'
 let focusPrimaryWindow = (): void => {}
@@ -79,8 +81,11 @@ function developmentHostInspectPort(enabled: boolean): number | undefined {
   return port
 }
 
-function createWindow(preload: string): BrowserWindow {
+function createWindow(preload: string, title = 'AgentRouter'): BrowserWindow {
   const window = new BrowserWindow({
+    title,
+    icon: join(app.getAppPath(), 'renderer', 'agentrouter-icon.png'),
+    autoHideMenuBar: process.platform === 'win32',
     width: 1280,
     height: 840,
     minWidth: 880,
@@ -94,6 +99,12 @@ function createWindow(preload: string): BrowserWindow {
       webSecurity: true,
     },
   })
+  window.on('page-title-updated', (event, pageTitle) => {
+    event.preventDefault()
+    window.setTitle(pageTitle.endsWith(' — DeepSeek Harness')
+      ? `${pageTitle.slice(0, -'DeepSeek Harness'.length)}AgentRouter`
+      : title)
+  })
   window.webContents.setWindowOpenHandler(() => ({ action: 'deny' }))
   window.webContents.on('will-navigate', (event, url) => {
     if (new URL(url).protocol !== `${SCHEME}:`) event.preventDefault()
@@ -150,6 +161,9 @@ async function main(): Promise<void> {
 
   const publishUpdate = (state: DesktopUpdateState): DesktopUpdateState => {
     updateState = state
+    const action = Menu.getApplicationMenu()?.getMenuItemById('product-update')
+    if (action) action.label = state.phase === 'available' || state.phase === 'ready'
+      ? `${messages.installAndRestart} (${state.version})` : messages.checkUpdatesMenu
     for (const window of BrowserWindow.getAllWindows()) {
       window.webContents.send(DESKTOP_IPC.updatesState, state)
     }
@@ -158,7 +172,8 @@ async function main(): Promise<void> {
 
   const startHost = async (projectDir = activeProject): Promise<DesktopHostProcess> => {
     const next = new DesktopHostProcess(resources.node, projectDir, hostInspectPort)
-    await next.start()
+    try { await next.start() }
+    catch (error) { await next.stop().catch(() => undefined); throw error }
     return next
   }
   const hooks: DesktopProjectHooks = {
@@ -214,6 +229,9 @@ async function main(): Promise<void> {
   const updates = new DesktopUpdateCoordinator(
     publishUpdate,
     async () => {
+      if (host !== undefined && !await host.prepareRestart()) {
+        throw new Error('任务仍在运行，请在任务完成后再次更新。')
+      }
       shellInstallerOwnsQuit = true
       const active = host
       host = undefined
@@ -221,10 +239,18 @@ async function main(): Promise<void> {
     },
   )
 
+  const updateRoutes = createProductUpdateRoutes({ coordinator: updates, productVersion: app.getVersion(), history: updateHistory,
+    fetchHost: (request: Request) => {
+      if (!host) throw new Error('backend unavailable')
+      return host.fetch(request)
+    },
+  })
+
   protocol.handle(SCHEME, (request) => {
     const url = new URL(request.url)
     if (url.hostname === 'shell') return serveShellAsset(request)
     if (url.hostname !== 'app') return Promise.resolve(new Response(null, { status: 404 }))
+    if (updateRoutes.matches(request)) return updateRoutes.handle(request)
     const active = host
     if (active === undefined) return Promise.resolve(new Response('backend unavailable', { status: 503 }))
     return active.fetch(request)
@@ -272,6 +298,7 @@ async function main(): Promise<void> {
 
   const checkAndPrompt = async (manual: boolean): Promise<void> => {
     const state = await updates.check()
+    if (!manual) return
     if (state.phase === 'error') {
       if (manual) {
         await dialog.showMessageBox({
@@ -282,7 +309,7 @@ async function main(): Promise<void> {
       }
       return
     }
-    if (state.phase !== 'available') {
+    if (state.phase !== 'available' && state.phase !== 'ready') {
       if (manual) {
         await dialog.showMessageBox({
           type: 'info',
@@ -317,9 +344,8 @@ async function main(): Promise<void> {
       pluginWindow.focus()
       return
     }
-    pluginWindow = createWindow(managementPreload)
+    pluginWindow = createWindow(managementPreload, messages.pluginWindowTitle.replace('DeepSeek Harness', 'AgentRouter'))
     pluginWindow.setSize(900, 620)
-    pluginWindow.setTitle(messages.pluginWindowTitle)
     pluginWindow.once('ready-to-show', () => { pluginWindow?.show() })
     pluginWindow.once('closed', () => { pluginWindow = undefined })
     void pluginWindow.loadURL(`${SCHEME}://shell/plugin-manager.html`)
@@ -334,7 +360,7 @@ async function main(): Promise<void> {
         enabled: development === undefined,
         click: openPluginWindow,
       },
-      { label: messages.checkUpdatesMenu, click: () => { void checkAndPrompt(true) } },
+      { id: 'product-update', label: messages.checkUpdatesMenu, click: () => { void checkAndPrompt(true) } },
       { type: 'separator' },
       { role: 'quit' },
     ],
@@ -392,6 +418,21 @@ if (ownsDesktopInstance) void app.whenReady().then(main).catch(async (error: unk
   if (diagnosticFile !== undefined) {
     await writeFile(diagnosticFile, `${error instanceof Error ? error.stack ?? message : message}\n`).catch(() => undefined)
   }
-  dialog.showErrorBox(resolveDesktopLocale(app.getLocale()).messages.startupFailed, message)
+  const messages = resolveDesktopLocale(app.getLocale()).messages
+  const recovery = await dialog.showMessageBox({ type: 'error', title: messages.startupFailed,
+    message, buttons: [messages.checkUpdatesMenu, messages.later], defaultId: 0, cancelId: 1 })
+  if (recovery.response === 0) {
+    const updates = new DesktopUpdateCoordinator(state => state)
+    const state = await updates.check()
+    if (state.phase === 'available') {
+      const choice = await dialog.showMessageBox({ type: 'info', title: messages.updateTitle,
+        message: messages.updateAvailable, detail: state.version,
+        buttons: [messages.installAndRestart, messages.later], defaultId: 0, cancelId: 1 })
+      if (choice.response === 0 && (await updates.install()).phase !== 'error') return
+    } else {
+      await dialog.showMessageBox({ type: 'info', title: messages.updateCheckTitle,
+        message: state.phase === 'error' ? (state.message ?? messages.unknownError) : messages.updateCurrent })
+    }
+  }
   app.exit(1)
 })
diff --git a/apps/desktop/src/project-manager.ts b/apps/desktop/src/project-manager.ts
index b9b94f7..be80437 100644
--- a/apps/desktop/src/project-manager.ts
+++ b/apps/desktop/src/project-manager.ts
@@ -22,6 +22,8 @@ import {
   writeSync,
 } from 'node:fs'
 import { basename, delimiter, dirname, isAbsolute, join, relative, resolve, sep } from 'node:path'
+import { load as loadYaml, YAMLException } from 'js-yaml'
+import { valid } from 'semver'
 import {
   DESKTOP_PACKAGES_DIR,
   DESKTOP_PACKAGE_SET_FILE,
@@ -41,6 +43,7 @@ const DESKTOP_PROJECT_FILES = [
   'pnpm-lock.yaml',
   'pnpm-workspace.yaml',
   'desktop-release.json',
+  'cordis.patch.yml',
   DESKTOP_PACKAGE_SET_FILE,
 ] as const
 
@@ -48,6 +51,7 @@ const DESKTOP_PROJECT_FILES = [
 export interface DesktopPluginRecord {
   readonly name: string
   readonly version: string
+  readonly updateOwner?: 'product'
 }
 
 /** Installed desktop project manifest slice. */
@@ -109,6 +113,15 @@ const VERSION_PATTERN = /^[0-9A-Za-z][0-9A-Za-z.+_-]*$/u
 const MAX_PNPM_DIAGNOSTIC_BYTES = 64 * 1024
 const DESKTOP_REGISTRY = 'https://registry.npmjs.org/'
 
+class PnpmCommandError extends Error {
+  readonly offlineCacheMiss: boolean
+
+  constructor(status: string, diagnostics: string) {
+    super(`desktop project: pnpm exited with ${status}${diagnostics.trim() === '' ? '' : `: ${diagnostics.trim()}`}`)
+    this.offlineCacheMiss = /\bERR_PNPM_NO_OFFLINE_(?:META|TARBALL)\b/u.test(diagnostics)
+  }
+}
+
 function errorOf(reason: unknown, fallback: string): Error {
   return reason instanceof Error ? reason : new Error(fallback)
 }
@@ -121,7 +134,7 @@ function readJson(path: string): unknown {
   return JSON.parse(readFileSync(path, 'utf8'))
 }
 
-function workspaceFile(overrides: Readonly<Record<string, string>> = {}): string {
+function workspaceFile(overrides: Readonly<Record<string, string>> = {}, managed: DesktopRelease['managedPlugins'] = []): string {
   const entries = Object.entries(overrides).sort(([left], [right]) => left.localeCompare(right))
   const overrideSection = entries.length === 0
     ? ''
@@ -130,7 +143,8 @@ function workspaceFile(overrides: Readonly<Record<string, string>> = {}): string
   const coreBuildKey = coreBuildSpec === undefined
     ? CORE_BUILD_PACKAGE
     : `${CORE_BUILD_PACKAGE}@${coreBuildSpec.replace('file:./', 'file:')}`
-  return `packages:\n  - .\n\n${overrideSection}${WORKSPACE_SETTINGS}allowBuilds:\n  node-pty: true\n  koffi: true\n  fs-ext: true\n  ${JSON.stringify(coreBuildKey)}: true\n  '@google/genai': false\n  protobufjs: false\n  node-addon-require-builtin: false\n`
+  const approved = managed.length ? `minimumReleaseAgeExclude:\n${managed.map(plugin => `  - ${JSON.stringify(`${plugin.name}@${plugin.version}`)}`).join('\n')}\n` : ''
+  return `packages:\n  - .\n\n${overrideSection}${WORKSPACE_SETTINGS}${approved}allowBuilds:\n  node-pty: true\n  koffi: true\n  fs-ext: true\n  ${JSON.stringify(coreBuildKey)}: true\n  '@google/genai': false\n  protobufjs: false\n  node-addon-require-builtin: false\n`
 }
 
 function releaseFile(projectDir: string): DesktopRelease {
@@ -265,7 +279,7 @@ function projectManifest(projectDir: string): DesktopProjectManifest {
   const expectedOverrides = desktopCorePackageOverrides(packageSet)
   if (manifest.dependencies[DSH_PACKAGE] !== desktopDshPackageSpec(packageSet)
     || Object.entries(expectedOverrides).some(([name, spec]) => manifest.dependencies[name] !== spec)
-    || readFileSync(join(projectDir, 'pnpm-workspace.yaml'), 'utf8') !== workspaceFile(expectedOverrides)) {
+    || readFileSync(join(projectDir, 'pnpm-workspace.yaml'), 'utf8') !== workspaceFile(expectedOverrides, releaseFile(projectDir).managedPlugins)) {
     throw new Error(`desktop project: core package mapping does not match ${DESKTOP_PACKAGE_SET_FILE}`)
   }
   return manifest
@@ -285,7 +299,85 @@ function profilePluginNames(projectDir: string): readonly string[] {
 }
 
 function pluginRecords(projectDir: string): readonly DesktopPluginRecord[] {
-  return profilePluginNames(projectDir).map(name => inspectPlugin(projectDir, name))
+  const managed = releaseFile(projectDir).managedPlugins ?? []
+  return profilePluginNames(projectDir).map(name => ({ ...inspectPlugin(projectDir, name),
+    ...(managed.some(plugin => plugin.name === name) ? { updateOwner: 'product' as const } : {}),
+  }))
+}
+
+function legacyDependencyVersion(projectDir: string, name: string, specifier: unknown): string | undefined {
+  if (typeof specifier !== 'string') return undefined
+  if (valid(specifier) === specifier) return specifier
+  const lockPath = join(projectDir, 'pnpm-lock.yaml')
+  if (!existsSync(lockPath)) return undefined
+  try {
+    const value = loadYaml(readFileSync(lockPath, 'utf8'))
+    if (!isRecord(value) || !isRecord(value.importers) || !isRecord(value.importers['.'])) return undefined
+    const importer = value.importers['.']
+    if (!isRecord(importer.dependencies) || !isRecord(importer.dependencies[name])) return undefined
+    const entry = importer.dependencies[name]
+    // A leftover lock must not silently select a different dependency. pnpm can
+    // normalize local paths, so compare those against the same profile directory.
+    if (entry.specifier !== specifier && !(typeof entry.specifier === 'string'
+      && entry.specifier.startsWith('file:') && specifier.startsWith('file:')
+      && resolve(projectDir, entry.specifier.slice(5)) === resolve(projectDir, specifier.slice(5)))) return undefined
+    if (typeof entry.version !== 'string') return undefined
+    const locked = entry.version.split('(')[0]!
+    if (valid(locked) === locked) return locked
+    if (!isRecord(value.packages)) return undefined
+    const exact = value.packages[`${name}@${locked}`]
+    if (isRecord(exact) && (exact.name === undefined || exact.name === name)
+      && typeof exact.version === 'string' && valid(exact.version) === exact.version) return exact.version
+  } catch (error) {
+    if (!(error instanceof YAMLException)) throw error
+  }
+  return undefined
+}
+
+/** Public profile format used by the preceding community Desktop distribution.
+ * Recognition is structural, with no client-version allowlist. Never reinterpret
+ * a damaged official release as a legacy installation.
+ */
+function legacyProfile(projectDir: string): { plugins: DesktopPluginRecord[]; bundles: string[] } | undefined {
+  if (!existsSync(projectDir) || existsSync(join(projectDir, 'desktop-release.json'))) return undefined
+  const value = readJson(join(projectDir, 'package.json'))
+  if (!isRecord(value) || value.name !== 'dsh-profile-desktop' || !isRecord(value.dependencies)
+    || !isRecord(value.dsh) || !isRecord(value.dsh.profile) || !Array.isArray(value.dsh.profile.bundles)
+    || !value.dsh.profile.bundles.every(bundle => typeof bundle === 'string')) {
+    throw new Error('无法识别已有 desktop 配置；原数据已保留，请检查配置后重试。')
+  }
+  let bundles = value.dsh.profile.bundles as string[]
+  if (!DESKTOP_PROFILE_BUNDLES.every((bundle, index) => bundles[index] === bundle)) {
+    throw new Error('已有 desktop 配置缺少标准 DSH 组合；原数据已保留，请检查配置后重试。')
+  }
+  const dependencies = value.dependencies
+  // Early preinstallation replaced these product bundles, but some profiles
+  // retained their enabled names after removing the packages. Only reconcile
+  // those known leftovers when the replacement and its receipt are both present.
+  const preinstalled = join(projectDir, 'agentrouter-preinstalled.json')
+  if (typeof dependencies['@agentrouter-top/dsh-codex'] === 'string'
+    && bundles.includes('@agentrouter-top/dsh-codex') && existsSync(preinstalled)) {
+    const receipt = readJson(preinstalled)
+    if (isRecord(receipt) && receipt.package === '@agentrouter-top/dsh-codex') {
+      const replaced = new Set(['@agentrouter-top/dsh-plugin', 'relay-dsh-plugin-codex'])
+      bundles = bundles.filter(name => !replaced.has(name) || Object.hasOwn(dependencies, name)
+        || existsSync(join(projectDir, 'node_modules', name, 'package.json')))
+    }
+  }
+  const names = [...new Set([...Object.keys(dependencies), ...bundles.slice(DESKTOP_PROFILE_BUNDLES.length)])]
+    .filter(name => !(DESKTOP_PROFILE_BUNDLES as readonly string[]).includes(name))
+  for (const name of names) assertPackageName(name)
+  return { plugins: names.map(name => {
+    if (existsSync(join(projectDir, 'node_modules', name, 'package.json'))) return inspectPlugin(projectDir, name)
+    // Preinstalled community bundles may live in the old executable's module
+    // fallback rather than this profile. Its public manifest records their exact
+    // registry identities. Never resolve a moving tag/range during migration.
+    const version = legacyDependencyVersion(projectDir, name, dependencies[name])
+    if (typeof version !== 'string' || valid(version) !== version) {
+      throw new Error(`无法确认已有插件 ${name} 的精确版本；请先通过原客户端的插件管理修复安装。`)
+    }
+    return { name, version }
+  }), bundles }
 }
 
 function writeProfilePlugins(projectDir: string, plugins: readonly DesktopPluginRecord[]): void {
@@ -387,7 +479,8 @@ export class DesktopProjectManager {
   /** Read the release version applied to the active desktop project. */
   releaseVersion(): string {
     if (!existsSync(this.paths.profile)) throw new Error('desktop project: active profile is not installed')
-    return releaseFile(this.paths.profile).version
+    const release = releaseFile(this.paths.profile)
+    return release.productVersion ?? release.version
   }
 
   /** Install or reconcile the active project to the Electron package's exact release. */
@@ -397,30 +490,83 @@ export class DesktopProjectManager {
       verifySeedIntegrity(seedDir)
       const target = releaseFile(seedDir)
       verifyDesktopCorePackageSet(seedDir, target.version)
-      if (target.version !== electronVersion) {
+      const productVersion = target.productVersion ?? target.version
+      if (productVersion !== electronVersion) {
         throw new Error(`desktop project: seed ${target.version} does not match Electron ${electronVersion}`)
       }
-      if (existsSync(this.paths.profile) && this.releaseVersion() === target.version
+      const legacy = legacyProfile(this.paths.profile)
+      const currentRelease = !legacy && existsSync(this.paths.profile) && this.releaseVersion() === productVersion
         && this.dshVersion() === target.version
-        && this.installedPackageVersion(DESKTOP_HOST_PACKAGE) === target.version) {
+        && this.installedPackageVersion(DESKTOP_HOST_PACKAGE) === target.version
+      if (currentRelease) {
         verifyDesktopCorePackageSet(this.paths.profile, target.version)
-        return false
+        // An external install can rebuild the same release with another pnpm store.
+        // Compare the actual link owner, not a pnpm version allowlist.
+        if (await this.usesBundledStore()) return false
       }
       this.mergeSeedPnpmState(seedDir)
       const stagingProfile = this.newStagingProfile()
       try {
-        if (existsSync(this.paths.profile)) {
-          const plugins = pluginRecords(this.paths.profile)
+        if (currentRelease) {
+          // Repair only the dependency layout. Keep the user's exact lock and bundles,
+          // including managed plugins they removed, disabled plugins and their config.
+          copyMetadata(this.paths.profile, stagingProfile)
+          await this.prepareFromCache(stagingProfile, ['install', '--frozen-lockfile', '--trust-lockfile'])
+        } else if (existsSync(this.paths.profile)) {
+          const previous = legacy ? undefined : releaseFile(this.paths.profile)
+          const previousManifest = legacy ? undefined : projectManifest(this.paths.profile)
+          const core = previous ? new Set(readDesktopCorePackageSet(this.paths.profile, previous.version).packages.map(pkg => pkg.name)) : undefined
+          const installed = legacy?.plugins ?? Object.keys(previousManifest!.dependencies)
+            .filter(name => !core!.has(name)).map(name => inspectPlugin(this.paths.profile, name))
+          const previousBundles = legacy?.bundles ?? previousManifest!.dsh.profile.bundles
+          const disabled = new Set(installed.filter(plugin => !previousBundles.includes(plugin.name)).map(plugin => plugin.name))
+          const managed = target.managedPlugins ?? []
+          const userPlugins = installed.filter(plugin => !managed.some(entry => entry.name === plugin.name))
+          // An explicitly removed managed plugin stays removed. A newly introduced
+          // managed package is enabled once; ordinary user plugins keep their versions.
+          const removed = managed.filter(plugin => (previous?.managedPlugins?.some(entry => entry.name === plugin.name)
+            || (legacy && existsSync(join(this.paths.profile, 'agentrouter-preinstalled.json'))))
+            && !installed.some(entry => entry.name === plugin.name))
+          const plugins = [...managed.filter(plugin => !removed.includes(plugin)), ...userPlugins]
           copyMetadata(seedDir, stagingProfile)
           await this.runPnpm(stagingProfile, ['install', '--offline', '--frozen-lockfile', '--trust-lockfile'])
-          if (plugins.length > 0) {
-            await this.runPnpm(stagingProfile, [
+          if (userPlugins.length > 0) {
+            await this.prepareFromCache(stagingProfile, [
               'add',
-              ...plugins.map(plugin => `${plugin.name}@${plugin.version}`),
+              ...userPlugins.map(plugin => `${plugin.name}@${plugin.version}`),
               '--save-exact',
-              '--offline',
+              '--lockfile-only',
+              // The preceding install verified this seed's lock. Keep normal
+              // resolver checks for added packages without rechecking every seed entry.
+              '--trust-lockfile',
             ])
-            writeProfilePlugins(stagingProfile, plugins)
+          }
+          for (const plugin of removed) await this.runPnpm(stagingProfile, ['remove', plugin.name, '--offline', '--lockfile-only', '--trust-lockfile'])
+          if (userPlugins.length > 0 || removed.length > 0) {
+            await this.prepareFromCache(stagingProfile, ['install', '--frozen-lockfile', '--trust-lockfile'])
+          }
+          writeProfilePlugins(stagingProfile, plugins)
+          if (disabled.size > 0) {
+            const manifest = projectManifest(stagingProfile)
+            writeJson(join(stagingProfile, 'package.json'), { ...manifest,
+              dsh: { ...manifest.dsh, profile: { ...manifest.dsh.profile,
+                bundles: manifest.dsh.profile.bundles.filter(name => !disabled.has(name)) } } })
+          }
+          const userPatch = join(this.paths.profile, 'cordis.patch.yml')
+          if (existsSync(userPatch)) copyFileSync(userPatch, join(stagingProfile, 'cordis.patch.yml'))
+          if (legacy) {
+            // Retain installed-but-disabled bundles and standalone profile config.
+            const config = join(this.paths.profile, 'cordis.yml')
+            if (existsSync(config)) copyFileSync(config, join(stagingProfile, 'cordis.yml'))
+            // Keep a durable metadata backup beyond the next transaction's rollback
+            // slot. Do not follow module fallback links into another installation.
+            const backup = join(this.paths.root, 'legacy-profile-backups', randomUUID())
+            cpSync(this.paths.profile, backup, { recursive: true, filter: path =>
+              !relative(this.paths.profile, path).split(sep).includes('node_modules')
+              && !lstatSync(path).isSymbolicLink() })
+            writeJson(join(stagingProfile, 'agentrouter-legacy-migration.json'), {
+              schemaVersion: 1, source: 'dsh-profile-desktop', productVersion, backup,
+            })
           }
         } else {
           copyMetadata(seedDir, stagingProfile)
@@ -441,10 +587,18 @@ export class DesktopProjectManager {
     await this.withLock(async () => {
       this.recover()
       if (!existsSync(this.paths.profile)) throw new Error('desktop project: active profile is not installed')
-      verifyDesktopCorePackageSet(this.paths.profile, this.releaseVersion())
+      verifyDesktopCorePackageSet(this.paths.profile, releaseFile(this.paths.profile).version)
+      const managed = releaseFile(this.paths.profile).managedPlugins ?? []
+      const requested = mutation.type === 'plugin-add' ? packageNameFromSpec(mutation.spec) : mutation.name
+      const owned = managed.find(plugin => plugin.name === requested)
+      if (owned && (mutation.type === 'plugin-update'
+        || (mutation.type === 'plugin-add' && mutation.spec !== `${owned.name}@${owned.version}`))) {
+        throw new Error('此插件随客户端一起更新，请使用客户端更新入口。')
+      }
       const stagingProfile = this.newStagingProfile()
       try {
         copyMetadata(this.paths.profile, stagingProfile)
+        await this.prepareFromCache(stagingProfile, ['install', '--frozen-lockfile', '--trust-lockfile'])
         await this.applyMutation(stagingProfile, mutation)
         await hooks.healthCheck(stagingProfile)
         await this.activate(stagingProfile, hooks)
@@ -461,12 +615,48 @@ export class DesktopProjectManager {
     return path
   }
 
+  private async usesBundledStore(): Promise<boolean> {
+    let modules: unknown
+    try {
+      modules = loadYaml(readFileSync(join(this.paths.profile, 'node_modules', '.modules.yaml'), 'utf8'))
+    } catch (error) {
+      if ((error as NodeJS.ErrnoException).code === 'ENOENT' || error instanceof YAMLException) return false
+      throw error
+    }
+    if (!isRecord(modules) || typeof modules.storeDir !== 'string' || !isAbsolute(modules.storeDir)
+      || modules.nodeLinker !== 'hoisted') return false
+    // This read-only pnpm command uses exactly the same bundled runtime and private
+    // configuration as installation. Do not guess v10/v11 directory conventions.
+    const expected = (await this.runPnpm(this.paths.root, ['store', 'path', '--silent'])).trim()
+    if (!isAbsolute(expected)) throw new Error('desktop project: bundled pnpm did not report an absolute store path')
+    return relative(resolve(modules.storeDir), resolve(expected)) === ''
+  }
+
+  private async prepareFromCache(projectDir: string, args: readonly string[]): Promise<void> {
+    try {
+      await this.runPnpm(projectDir, [...args, '--offline'])
+    } catch (error) {
+      if (!(error instanceof PnpmCommandError) || !error.offlineCacheMiss) throw error
+      // A new store format cannot read packages cached by the old pnpm. Only fetch
+      // missing user dependencies in staging; never change or delete the old graph.
+      const network = ['--prefer-offline', '--fetch-retries=1', '--fetch-timeout=30000']
+      if (args[0] === 'install') {
+        // Fetching and linking in one incremental operation can strand pnpm 11
+        // workers. Populate the store first, then require a fully offline install.
+        await this.runPnpm(projectDir, ['fetch', '--ignore-scripts', '--trust-lockfile', ...network])
+        await this.runPnpm(projectDir, [...args, '--offline'])
+      } else {
+        await this.runPnpm(projectDir, [...args, ...network])
+      }
+    }
+  }
+
   private async applyMutation(projectDir: string, mutation: DesktopProjectMutation): Promise<void> {
     switch (mutation.type) {
       case 'plugin-add': {
         const requestedName = packageNameFromSpec(mutation.spec)
         if (requestedName === undefined) throw new Error('desktop project: plugin package name is required')
-        await this.runPnpm(projectDir, ['add', mutation.spec, '--save-exact'])
+        await this.resolveMutation(projectDir, ['add', mutation.spec, '--save-exact'])
         const installed = inspectPlugin(projectDir, requestedName)
         const current = pluginRecords(projectDir).filter(plugin => plugin.name !== installed.name)
         writeProfilePlugins(
@@ -481,7 +671,7 @@ export class DesktopProjectManager {
           throw new Error(`desktop project: plugin ${JSON.stringify(mutation.name)} is not installed`)
         }
         const remaining = pluginRecords(projectDir).filter(plugin => plugin.name !== mutation.name)
-        await this.runPnpm(projectDir, ['remove', mutation.name])
+        await this.resolveMutation(projectDir, ['remove', mutation.name])
         writeProfilePlugins(projectDir, remaining)
         return
       }
@@ -491,7 +681,7 @@ export class DesktopProjectManager {
         if (!profilePluginNames(projectDir).includes(mutation.name)) {
           throw new Error(`desktop project: plugin ${JSON.stringify(mutation.name)} is not installed`)
         }
-        await this.runPnpm(projectDir, ['add', `${mutation.name}@${mutation.version}`, '--save-exact'])
+        await this.resolveMutation(projectDir, ['add', `${mutation.name}@${mutation.version}`, '--save-exact'])
         {
           const installed = inspectPlugin(projectDir, mutation.name)
           writeProfilePlugins(
@@ -505,6 +695,14 @@ export class DesktopProjectManager {
     }
   }
 
+  private async resolveMutation(projectDir: string, args: readonly string[]): Promise<void> {
+    // pnpm 11 can leave workers alive after a hoisted incremental add/remove on
+    // the full Desktop graph. Separate resolution and frozen materialization
+    // through supported CLI options; never infer success from its progress text.
+    await this.runPnpm(projectDir, [...args, '--lockfile-only'])
+    await this.prepareFromCache(projectDir, ['install', '--frozen-lockfile', '--trust-lockfile'])
+  }
+
   private mergeSeedPnpmState(seedDir: string): void {
     const transactionRoot = join(this.paths.staging, randomUUID())
     const extractedStore = join(transactionRoot, 'store')
@@ -526,6 +724,7 @@ export class DesktopProjectManager {
     writeJson(this.paths.pending, pending)
     await hooks.beforeActivate()
     let activeMoved = false
+    let stagingActivated = false
     try {
       removeOwnedDirectory(this.paths.rollback)
       mkdirSync(dirname(this.paths.rollback), { recursive: true, mode: 0o700 })
@@ -537,10 +736,11 @@ export class DesktopProjectManager {
       mkdirSync(dirname(this.paths.profile), { recursive: true, mode: 0o700 })
       writeJson(this.paths.pending, { ...pending, step: 'staging-activated' } satisfies DesktopPendingTransaction)
       renameSync(stagingProfile, this.paths.profile)
+      stagingActivated = true
       await hooks.afterActivate()
       unlinkSync(this.paths.pending)
     } catch (error) {
-      if (existsSync(this.paths.profile)) removeOwnedDirectory(this.paths.profile)
+      if (stagingActivated && existsSync(this.paths.profile)) removeOwnedDirectory(this.paths.profile)
       if (activeMoved && existsSync(this.paths.rollback)) renameSync(this.paths.rollback, this.paths.profile)
       if (existsSync(this.paths.pending)) unlinkSync(this.paths.pending)
       await hooks.afterActivate().catch(() => undefined)
@@ -548,7 +748,7 @@ export class DesktopProjectManager {
     }
   }
 
-  private async runPnpm(projectDir: string, args: readonly string[]): Promise<void> {
+  private async runPnpm(projectDir: string, args: readonly string[]): Promise<string> {
     const [command, ...commandArgs] = args
     if (command === undefined) throw new Error('desktop project: pnpm command is required')
     for (const path of [this.paths.root, this.paths.pnpm.store, this.paths.pnpm.cache,
@@ -560,7 +760,7 @@ export class DesktopProjectManager {
     const inherited = Object.fromEntries(Object.entries(process.env).filter(([name]) => (
       !/^DSH_DESKTOP_/u.test(name) && !/^(?:npm|pnpm|corepack)_/iu.test(name)
     )))
-    await new Promise<void>((settle, reject) => {
+    return new Promise<string>((settle, reject) => {
       const child = spawn(this.runtime.node, [
         this.runtime.pnpm,
         `--config.registry=${DESKTOP_REGISTRY}`,
@@ -571,8 +771,12 @@ export class DesktopProjectManager {
         ...commandArgs,
       ], {
         cwd: projectDir,
+        windowsHide: true,
         env: {
           ...inherited,
+          // Package operations own only staging directories and have no interactive
+          // terminal. pnpm may rebuild those modules without a hidden TTY prompt.
+          CI: 'true',
           COREPACK_HOME: this.paths.pnpm.home,
           NPM_CONFIG_REGISTRY: DESKTOP_REGISTRY,
           NPM_CONFIG_STORE_DIR: this.paths.pnpm.store,
@@ -599,12 +803,14 @@ export class DesktopProjectManager {
         return
       }
       let diagnostics = ''
+      let stdout = ''
       let completed = false
       const appendDiagnostics = (chunk: string): void => {
         diagnostics = (diagnostics + chunk).slice(-MAX_PNPM_DIAGNOSTIC_BYTES)
       }
       child.stdout.setEncoding('utf8')
       child.stdout.on('data', appendDiagnostics)
+      child.stdout.on('data', (chunk: string) => { stdout = (stdout + chunk).slice(-MAX_PNPM_DIAGNOSTIC_BYTES) })
       child.stderr.setEncoding('utf8')
       child.stderr.on('data', appendDiagnostics)
       const complete = (settleChild: () => void): void => {
@@ -622,12 +828,10 @@ export class DesktopProjectManager {
       child.once('close', (code, signal) => {
         complete(() => {
           if (code === 0) {
-            settle()
+            settle(stdout)
             return
           }
-          reject(new Error(
-            `desktop project: pnpm exited with ${String(code ?? signal)}${diagnostics.trim() === '' ? '' : `: ${diagnostics.trim()}`}`,
-          ))
+          reject(new PnpmCommandError(String(code ?? signal), diagnostics))
         })
       })
     })
@@ -690,13 +894,14 @@ export function createSeedMetadata(seedDir: string, release: DesktopRelease): vo
     name: PROJECT_NAME,
     private: true,
     version: '0.0.0',
-    dependencies: desktopCorePackageOverrides(packageSet),
-    dsh: { profile: { bundles: [...DESKTOP_PROFILE_BUNDLES] } },
+    dependencies: { ...desktopCorePackageOverrides(packageSet),
+      ...Object.fromEntries((release.managedPlugins ?? []).map(plugin => [plugin.name, plugin.version])) },
+    dsh: { profile: { bundles: [...DESKTOP_PROFILE_BUNDLES, ...(release.managedPlugins ?? []).map(plugin => plugin.name)] } },
   }
   writeJson(join(seedDir, 'package.json'), manifest)
   writeFileSync(
     join(seedDir, 'pnpm-workspace.yaml'),
-    workspaceFile(desktopCorePackageOverrides(packageSet)),
+    workspaceFile(desktopCorePackageOverrides(packageSet), release.managedPlugins),
     { mode: 0o600 },
   )
   writeJson(join(seedDir, 'desktop-release.json'), release)
diff --git a/apps/desktop/src/release.ts b/apps/desktop/src/release.ts
index f78d922..7587a17 100644
--- a/apps/desktop/src/release.ts
+++ b/apps/desktop/src/release.ts
@@ -6,8 +6,10 @@ import { DESKTOP_HOST_PROTOCOL_VERSION } from './host-protocol.ts'
 /** Release facts embedded in the seed and copied into the active desktop project. */
 export interface DesktopRelease {
   readonly schemaVersion: 1
-  /** Exact version used by both Electron and `@deepseek-ai/dsh`. */
+  /** Exact DSH core version; product releases may retain this version. */
   readonly version: string
+  readonly productVersion?: string
+  readonly managedPlugins?: readonly { readonly name: string; readonly version: string }[]
   readonly hostProtocolVersion: typeof DESKTOP_HOST_PROTOCOL_VERSION
   readonly nodeVersion: string
   readonly pnpmVersion: string
@@ -25,9 +27,21 @@ export function parseDesktopRelease(value: unknown): DesktopRelease {
     || typeof value.pnpmVersion !== 'string' || valid(value.pnpmVersion) === null) {
     throw new Error('dsh desktop: invalid desktop release metadata')
   }
+  if (value.productVersion !== undefined && (typeof value.productVersion !== 'string' || valid(value.productVersion) === null)) {
+    throw new Error('dsh desktop: invalid product version')
+  }
+  const managed = value.managedPlugins
+  if (managed !== undefined && (value.productVersion === undefined || !Array.isArray(managed)
+    || managed.some(entry => !isRecord(entry) || entry.name !== '@agentrouter-top/dsh-codex'
+      || typeof entry.version !== 'string' || valid(entry.version) === null)
+    || new Set(managed.map(entry => entry.name)).size !== managed.length)) {
+    throw new Error('dsh desktop: invalid managed plugin release')
+  }
   return {
     schemaVersion: 1,
     version: value.version,
+    ...(value.productVersion === undefined ? {} : { productVersion: value.productVersion as string }),
+    ...(managed === undefined ? {} : { managedPlugins: managed as DesktopRelease['managedPlugins'] }),
     hostProtocolVersion: DESKTOP_HOST_PROTOCOL_VERSION,
     nodeVersion: value.nodeVersion,
     pnpmVersion: value.pnpmVersion,
diff --git a/apps/desktop/src/update-coordinator.ts b/apps/desktop/src/update-coordinator.ts
index c31dcc3..d4efa5c 100644
--- a/apps/desktop/src/update-coordinator.ts
+++ b/apps/desktop/src/update-coordinator.ts
@@ -1,24 +1,31 @@
 /** One Electron release stream for the version-bound shell and dsh seed. */
-
-import { existsSync } from 'node:fs'
+import { existsSync, readFileSync } from 'node:fs'
 import { join } from 'node:path'
 import { app } from 'electron'
 import electronUpdater, { type AppUpdater } from 'electron-updater'
 import type { DesktopUpdateState } from './ipc.ts'
+import { configurePinnedUpdater } from './agentrouter-update-signature.mjs'
 const { autoUpdater } = electronUpdater
 
-/** Checks, downloads, and installs one complete Desktop release. */
+interface UpdateDetail {
+  phase: 'idle' | 'checking' | 'current' | 'available' | 'downloading' | 'ready' | 'restarting' | 'error'
+  version?: string
+  checkedAt?: string
+  progress?: { transferred: number; total: number; percent: number }
+  error?: string
+  releaseInfo?: unknown
+}
+
+/** The native menu and public update page share this single updater. */
 export class DesktopUpdateCoordinator {
   private availableVersion: string | undefined
+  private downloaded: string[] | undefined
+  private detail: UpdateDetail = { phase: 'idle' }
   private checkOperation: Promise<DesktopUpdateState> | undefined
+  private downloadOperation: Promise<DesktopUpdateState> | undefined
   private installOperation: Promise<DesktopUpdateState> | undefined
+  private readonly signatureCheck: ReturnType<typeof configurePinnedUpdater> | undefined
 
-  /**
-   * @param publish - state sink for every desktop window.
-   * @param beforeRestart - stop application-owned processes before replacement.
-   * @param updater - Electron artifact updater; replaceable for tests.
-   * @param enabled - whether this packaged process carries updater configuration.
-   */
   constructor(
     private readonly publish: (state: DesktopUpdateState) => DesktopUpdateState,
     private readonly beforeRestart: () => Promise<void> = async () => {},
@@ -29,67 +36,132 @@ export class DesktopUpdateCoordinator {
   ) {
     this.updater.autoDownload = false
     this.updater.autoInstallOnAppQuit = false
+    const signaturePath = app.isPackaged ? join(app.getAppPath(), 'update-signing.json') : undefined
+    if (signaturePath !== undefined && existsSync(signaturePath)) {
+      const signing = JSON.parse(readFileSync(signaturePath, 'utf8'))
+      this.signatureCheck = configurePinnedUpdater(this.updater, signing, () => this.availableVersion)
+    }
+    this.updater.on?.('download-progress', (progress) => {
+      if (this.detail.phase !== 'downloading') return
+      const { transferred, total, percent } = progress
+      if ([transferred, total, percent].every(Number.isFinite) && transferred >= 0 && total > 0) {
+        this.detail.progress = { transferred, total, percent: Math.max(0, Math.min(100, percent)) }
+      }
+    })
+    this.updater.on?.('error', () => {})
+  }
+
+  status() {
+    return { ...this.detail, version: this.availableVersion,
+      canDownload: !!this.availableVersion && !this.downloaded && !this.checkOperation && !this.downloadOperation && !this.installOperation,
+      canInstall: !!this.downloaded && this.detail.phase === 'ready' && !this.installOperation,
+      enabled: this.enabled() }
   }
 
-  /** Check the configured Desktop release stream and retain an available version. */
   async check(): Promise<DesktopUpdateState> {
-    if (this.installOperation !== undefined) return this.installOperation
-    if (this.checkOperation !== undefined) return this.checkOperation
+    if (this.installOperation) return this.installOperation
+    if (this.downloadOperation) return this.downloadOperation
+    if (this.checkOperation) return this.checkOperation
+    if (this.downloaded) return { phase: 'ready', version: this.availableVersion }
     this.checkOperation = this.doCheck().finally(() => { this.checkOperation = undefined })
     return this.checkOperation
   }
 
-  /** Wait for an in-flight check, then download and install its retained release. */
+  /** Download alone never stops work or quits the application. */
+  async download(expectedVersion: string | undefined = this.availableVersion): Promise<DesktopUpdateState> {
+    await this.checkOperation
+    this.assertVersion(expectedVersion)
+    if (this.installOperation) return this.installOperation
+    if (this.downloadOperation) return this.downloadOperation
+    if (this.downloaded) return { phase: 'ready', version: this.availableVersion }
+    this.downloadOperation = this.doDownload().finally(() => { this.downloadOperation = undefined })
+    return this.downloadOperation
+  }
+
+  /** Restart only the already downloaded, verified release the user selected. */
+  async restart(expectedVersion: string): Promise<DesktopUpdateState> {
+    this.assertVersion(expectedVersion)
+    if (this.detail.phase === 'restarting') return { phase: 'ready', version: this.availableVersion }
+    if (this.installOperation) return this.installOperation
+    if (!this.downloaded) throw new Error('desktop update: download the selected release first')
+    this.installOperation = this.doRestart().finally(() => { this.installOperation = undefined })
+    return this.installOperation
+  }
+
+  /** Keep the upstream native menu/recovery operation available. */
   async install(): Promise<DesktopUpdateState> {
-    if (this.installOperation !== undefined) return this.installOperation
+    if (this.detail.phase === 'restarting') return { phase: 'ready', version: this.availableVersion }
+    if (this.installOperation) return this.installOperation
     this.installOperation = (async () => {
       await this.checkOperation
-      return this.doInstall()
+      this.assertVersion(this.availableVersion)
+      if (this.downloadOperation) await this.downloadOperation
+      else if (!this.downloaded) await this.doDownload()
+      if (!this.downloaded) return { phase: 'error', version: this.availableVersion, message: this.detail.error }
+      return this.doRestart()
     })().finally(() => { this.installOperation = undefined })
     return this.installOperation
   }
 
+  private assertVersion(version: string | undefined): void {
+    if (!version || version !== this.availableVersion) throw new Error('desktop update: selected release has changed')
+  }
+
   private async doCheck(): Promise<DesktopUpdateState> {
+    this.detail = { ...this.detail, phase: 'checking', error: undefined, progress: undefined }
     this.publish({ phase: 'checking' })
     try {
       if (!this.enabled()) {
         this.availableVersion = undefined
+        this.detail.phase = 'idle'
         return this.publish({ phase: 'idle' })
       }
       const result = await this.updater.checkForUpdates()
       const version = result?.isUpdateAvailable === true ? result.updateInfo.version : undefined
       this.availableVersion = version
-      return version === undefined
-        ? this.publish({ phase: 'idle' })
-        : this.publish({ phase: 'available', version })
-    } catch (error) {
-      this.availableVersion = undefined
-      return this.publish({
-        phase: 'error',
-        message: error instanceof Error ? error.message : String(error),
-      })
+      this.detail = { phase: version ? 'available' : 'current', checkedAt: new Date().toISOString(), releaseInfo: result?.updateInfo }
+      return this.publish(version ? { phase: 'available', version } : { phase: 'idle' })
+    } catch {
+      this.detail = { ...this.detail, phase: 'error', error: '暂时无法检查更新，请检查网络后重试。' }
+      return this.publish({ phase: 'error', message: this.detail.error })
     }
   }
 
-  private async doInstall(): Promise<DesktopUpdateState> {
-    const version = this.availableVersion
-    if (version === undefined) {
-      throw new Error('desktop update: no verified update is available')
-    }
+  private async doDownload(): Promise<DesktopUpdateState> {
+    const version = this.availableVersion!
+    this.detail = { ...this.detail, phase: 'downloading', error: undefined, progress: undefined }
     this.publish({ phase: 'installing', version })
     try {
-      await this.updater.downloadUpdate()
-      this.availableVersion = undefined
-      const ready = this.publish({ phase: 'ready', version })
+      await this.signatureCheck?.beforeDownload()
+      const files = await this.updater.downloadUpdate()
+      await this.signatureCheck?.afterDownload(files)
+      this.downloaded = files
+      this.detail = { ...this.detail, phase: 'ready', progress: undefined }
+      return this.publish({ phase: 'ready', version })
+    } catch {
+      this.downloaded = undefined
+      this.detail = { ...this.detail, phase: 'error', progress: undefined, error: '更新下载或校验失败，请重新下载。' }
+      return this.publish({ phase: 'error', version, message: this.detail.error })
+    }
+  }
+
+  private async doRestart(): Promise<DesktopUpdateState> {
+    const version = this.availableVersion!
+    this.detail = { ...this.detail, phase: 'restarting', error: undefined }
+    try { await this.signatureCheck?.afterDownload(this.downloaded!) }
+    catch {
+      this.downloaded = undefined
+      this.detail = { ...this.detail, phase: 'error', error: '更新文件校验失败，请重新下载。' }
+      return this.publish({ phase: 'error', version, message: this.detail.error })
+    }
+    try {
       await this.beforeRestart()
-      this.updater.quitAndInstall(false, true)
-      return ready
-    } catch (error) {
-      return this.publish({
-        phase: 'error',
-        version,
-        message: error instanceof Error ? error.message : String(error),
-      })
+      this.detail = { ...this.detail, phase: 'restarting', error: undefined }
+      this.updater.quitAndInstall(true, true)
+      return { phase: 'ready', version }
+    } catch {
+      this.detail = { ...this.detail, phase: 'ready', error: '任务仍在运行，或暂时无法重启。请稍后再试。' }
+      return this.publish({ phase: 'error', version, message: this.detail.error })
     }
   }
 }
diff --git a/apps/desktop/tests/host-process.spec.ts b/apps/desktop/tests/host-process.spec.ts
index d3a08d7..fdc9cc1 100644
--- a/apps/desktop/tests/host-process.spec.ts
+++ b/apps/desktop/tests/host-process.spec.ts
@@ -75,6 +75,25 @@ afterEach(() => {
 })
 
 describe('desktop host process', () => {
+  it('asks the Host to reserve idle state and accepts a busy response without stopping it', async () => {
+    const project = projectWithHost(`
+function onRequestFrame() {}
+process.send({ type: 'ready', protocolVersion: 3, dshVersion: '0.1.5-rc.2' })
+let busy = true
+process.on('message', message => {
+  if (message.type !== 'prepare-restart') return
+  process.send({ type: 'restart-prepared', requestId: message.requestId, idle: !busy })
+  busy = false
+})
+`)
+    const host = new DesktopHostProcess(process.execPath, project)
+    try {
+      await host.start()
+      expect(await host.prepareRestart()).toBe(false)
+      expect(await host.prepareRestart()).toBe(true)
+    } finally { await host.stop() }
+  })
+
   it('carries raw request and response bytes and shuts the child down cleanly', async () => {
     const project = projectWithHost(`
 const bodies = new Map()
diff --git a/apps/desktop/tests/project-manager.spec.ts b/apps/desktop/tests/project-manager.spec.ts
index 27ac24f..8e6744e 100644
--- a/apps/desktop/tests/project-manager.spec.ts
+++ b/apps/desktop/tests/project-manager.spec.ts
@@ -85,10 +85,12 @@ function createTestSeedMetadata(seed: string, desktopRelease: DesktopRelease): v
 function writeFakePnpm(root: string): string {
   const path = join(root, 'pnpm.mjs')
   writeFileSync(path, String.raw`
-import { mkdirSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
+import { existsSync, mkdirSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
 import { dirname, join } from 'node:path'
 const args = process.argv.slice(2)
 const project = process.cwd()
+const storeDir = join(process.env.NPM_CONFIG_STORE_DIR, 'v11')
+if (args.includes('store')) { console.log(storeDir); process.exit(0) }
 const command = args.find(value => value === 'install' || value === 'add' || value === 'remove')
 const manifestPath = join(project, 'package.json')
 const manifest = JSON.parse(readFileSync(manifestPath, 'utf8'))
@@ -126,7 +128,12 @@ for (const [name, version] of Object.entries(manifest.dependencies)) {
   }
 }
 writeFileSync(join(project, 'pnpm-lock.yaml'), 'lockfileVersion: 9\n')
-if (process.env.TEST_PNPM_LOG) writeFileSync(process.env.TEST_PNPM_LOG, JSON.stringify({ args, env: process.env }))
+writeFileSync(join(project, 'node_modules', '.modules.yaml'), JSON.stringify({ storeDir, nodeLinker: 'hoisted', packageManager: 'pnpm@11.7.0' }))
+if (process.env.TEST_PNPM_LOG) {
+  const log = process.env.TEST_PNPM_LOG
+  const history = existsSync(log) ? JSON.parse(readFileSync(log, 'utf8')).history : []
+  writeFileSync(log, JSON.stringify({ args, env: process.env, history: [...history, args] }))
+}
 `)
   return path
 }
@@ -173,6 +180,185 @@ afterEach(async () => {
 })
 
 describe('desktop package policy', () => {
+  it.each([false, true])('migrates the preceding public profile transactionally (preinstalled fallback: %s)', async (fallback) => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const owned = '@agentrouter-top/dsh-codex'
+    const legacy = { name: 'dsh-profile-desktop', private: true,
+      dependencies: { [owned]: '0.7.0', '@fixture/ordinary': '2.0.0' },
+      dsh: { profile: { bundles: ['@deepseek-ai/dsh-base', '@deepseek-ai/dsh-web-app', owned, '@fixture/ordinary'] } } }
+    mkdirSync(paths.profile, { recursive: true })
+    writeFileSync(join(paths.profile, 'package.json'), JSON.stringify(legacy))
+    writeFileSync(join(paths.profile, 'cordis.patch.yml'), '# preserved user settings\n[]\n')
+    writeFileSync(join(paths.profile, 'cordis.yml'), '[]\n')
+    for (const [name, version] of fallback ? [] : Object.entries(legacy.dependencies)) {
+      const dir = join(paths.profile, 'node_modules', name)
+      mkdirSync(dir, { recursive: true })
+      writeFileSync(join(dir, 'package.json'), JSON.stringify({ name, version, dsh: { bundle: { patch: 'cordis.patch.yml' } } }))
+      writeFileSync(join(dir, 'cordis.patch.yml'), '[]\n')
+    }
+    const session = join(root, 'home', 'existing-session.jsonl')
+    writeFileSync(session, 'original conversation')
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, { ...release(), productVersion: '3.0.3', managedPlugins: [{ name: owned, version: '0.8.0' }] })
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await expect(manager.applyRelease(seed, '3.0.3', hooks({ healthCheck: async () => { throw new Error('probe failed') } }))).rejects.toThrow('probe failed')
+    expect(JSON.parse(readFileSync(join(paths.profile, 'package.json'), 'utf8'))).toEqual(legacy)
+    expect(readFileSync(session, 'utf8')).toBe('original conversation')
+    await manager.applyRelease(seed, '3.0.3', hooks())
+    expect(manager.listPlugins()).toEqual([
+      { name: owned, version: '0.8.0', updateOwner: 'product' }, { name: '@fixture/ordinary', version: '2.0.0' },
+    ])
+    expect(readFileSync(join(paths.profile, 'cordis.patch.yml'), 'utf8')).toContain('preserved user settings')
+    const receipt = JSON.parse(readFileSync(join(paths.profile, 'agentrouter-legacy-migration.json'), 'utf8'))
+    expect(JSON.parse(readFileSync(join(receipt.backup, 'package.json'), 'utf8'))).toEqual(legacy)
+    expect(existsSync(join(receipt.backup, 'node_modules'))).toBe(false)
+    expect(readFileSync(session, 'utf8')).toBe('original conversation')
+    await manager.applyRelease(seed, '3.0.3', hooks())
+    expect(readFileSync(session, 'utf8')).toBe('original conversation')
+    const manifestPath = join(paths.profile, 'package.json')
+    const manifest = JSON.parse(readFileSync(manifestPath, 'utf8'))
+    manifest.dsh.profile.bundles = manifest.dsh.profile.bundles.filter((name: string) => name !== '@fixture/ordinary')
+    writeFileSync(manifestPath, JSON.stringify(manifest))
+    const next = join(root, 'next-seed')
+    createTestSeedMetadata(next, { ...release(), productVersion: '3.0.4', managedPlugins: [{ name: owned, version: '0.8.1' }] })
+    archiveStore(next); writeIntegrity(next)
+    await manager.applyRelease(next, '3.0.4', hooks())
+    expect(JSON.parse(readFileSync(manifestPath, 'utf8')).dependencies['@fixture/ordinary']).toBe('2.0.0')
+    expect(manager.listPlugins().some(plugin => plugin.name === '@fixture/ordinary')).toBe(false)
+  })
+
+  it('reconciles dangling bundles replaced by the recorded product preinstallation', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const owned = '@agentrouter-top/dsh-codex'
+    const legacy = { name: 'dsh-profile-desktop', private: true,
+      dependencies: { [owned]: '0.7.1', '@fixture/ordinary': '2.0.0' },
+      dsh: { profile: { bundles: ['@deepseek-ai/dsh-base', '@deepseek-ai/dsh-web-app',
+        '@agentrouter-top/dsh-plugin', 'relay-dsh-plugin-codex', owned, '@fixture/ordinary'] } } }
+    mkdirSync(paths.profile, { recursive: true })
+    writeFileSync(join(paths.profile, 'package.json'), JSON.stringify(legacy))
+    writeFileSync(join(paths.profile, 'agentrouter-preinstalled.json'), JSON.stringify({ package: owned, version: '0.1.0-beta.16' }))
+    writeFileSync(join(paths.profile, 'cordis.patch.yml'), '# original configuration\n[]\n')
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, { ...release(), productVersion: '3.0.3', managedPlugins: [{ name: owned, version: '0.8.0' }] })
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await expect(manager.applyRelease(seed, '3.0.3', hooks({ healthCheck: async () => { throw new Error('probe failed') } }))).rejects.toThrow('probe failed')
+    expect(JSON.parse(readFileSync(join(paths.profile, 'package.json'), 'utf8'))).toEqual(legacy)
+    await expect(manager.applyRelease(seed, '3.0.3', hooks())).resolves.toBe(true)
+    expect(manager.listPlugins()).toEqual([{ name: owned, version: '0.8.0', updateOwner: 'product' }, { name: '@fixture/ordinary', version: '2.0.0' }])
+    const migration = JSON.parse(readFileSync(join(paths.profile, 'agentrouter-legacy-migration.json'), 'utf8'))
+    expect(JSON.parse(readFileSync(join(migration.backup, 'package.json'), 'utf8'))).toEqual(legacy)
+    expect(readFileSync(join(paths.profile, 'cordis.patch.yml'), 'utf8')).toBe('# original configuration\n[]\n')
+  })
+
+  it.each(['file', 'relative-file', 'peers', 'range'])('migrates the exact locked legacy plugin version (%s)', async (kind) => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const owned = '@agentrouter-top/dsh-codex'
+    const ordinary = '@agentrouter-top/dsh-plugin'
+    const fileSpec = kind === 'range' ? '^0.2.0' : `file:${join(paths.profile, 'archives', 'old-plugin.tgz')}`
+    const lockSpec = kind === 'range' ? '0.2.1' : 'file:archives/old-plugin.tgz'
+    const importerSpec = kind === 'relative-file' ? lockSpec : fileSpec
+    const reference = lockSpec + (kind === 'peers' ? '(example-peer@1.0.0)' : '')
+    const legacy = { name: 'dsh-profile-desktop', private: true,
+      dependencies: { [owned]: '0.7.0', [ordinary]: fileSpec },
+      dsh: { profile: { bundles: ['@deepseek-ai/dsh-base', '@deepseek-ai/dsh-web-app', owned] } } }
+    mkdirSync(paths.profile, { recursive: true })
+    writeFileSync(join(paths.profile, 'package.json'), JSON.stringify(legacy))
+    const lockText = `lockfileVersion: '9.0'\nimporters:\n  .:\n    dependencies:\n      ${JSON.stringify(ordinary)}:\n        specifier: ${JSON.stringify(importerSpec)}\n        version: ${JSON.stringify(reference)}\npackages:\n  ${JSON.stringify(`${ordinary}@${lockSpec}`)}:\n    resolution:\n      tarball: ${JSON.stringify(lockSpec)}\n    version: 0.2.1\n`
+    writeFileSync(join(paths.profile, 'pnpm-lock.yaml'), lockText)
+    writeFileSync(join(paths.profile, 'cordis.patch.yml'), '[]\n')
+    writeFileSync(join(paths.profile, 'cordis.yml'), '[]\n')
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, { ...release(), productVersion: '3.0.3', managedPlugins: [{ name: owned, version: '0.8.0' }] })
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await expect(manager.applyRelease(seed, '3.0.3', hooks({ healthCheck: async () => { throw new Error('probe failed') } }))).rejects.toThrow('probe failed')
+    expect(JSON.parse(readFileSync(join(paths.profile, 'package.json'), 'utf8'))).toEqual(legacy)
+    expect(readFileSync(join(paths.profile, 'pnpm-lock.yaml'), 'utf8')).toBe(lockText)
+    await expect(manager.applyRelease(seed, '3.0.3', hooks())).resolves.toBe(true)
+    expect(JSON.parse(readFileSync(join(paths.profile, 'package.json'), 'utf8')).dependencies[ordinary]).toBe('0.2.1')
+    expect(JSON.parse(readFileSync(join(paths.profile, 'node_modules', ordinary, 'package.json'), 'utf8')).version).toBe('0.2.1')
+    expect(manager.listPlugins()).toEqual([{ name: owned, version: '0.8.0', updateOwner: 'product' }])
+    const migration = JSON.parse(readFileSync(join(paths.profile, 'agentrouter-legacy-migration.json'), 'utf8'))
+    expect(readFileSync(join(migration.backup, 'pnpm-lock.yaml'), 'utf8')).toBe(lockText)
+  })
+
+  it.each(['missing-lock', 'malformed-lock', 'stale-specifier', 'other-package', 'missing-specifier'])('preserves an unresolved legacy profile (%s)', async (kind) => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const name = '@fixture/ordinary'
+    const spec = 'file:archives/plugin.tgz'
+    const legacy = { name: 'dsh-profile-desktop', private: true,
+      dependencies: kind === 'missing-specifier' ? {} : { [name]: spec },
+      dsh: { profile: { bundles: ['@deepseek-ai/dsh-base', '@deepseek-ai/dsh-web-app', name] } } }
+    mkdirSync(paths.profile, { recursive: true })
+    const manifestText = JSON.stringify(legacy)
+    writeFileSync(join(paths.profile, 'package.json'), manifestText)
+    const lockText = kind === 'malformed-lock' ? 'importers: [invalid' : JSON.stringify({
+      importers: { '.': { dependencies: { [name]: { specifier: kind === 'stale-specifier' ? 'file:another.tgz' : spec, version: spec } } } },
+      packages: { [`${kind === 'other-package' ? '@fixture/other' : name}@${spec}`]: { version: '2.0.0' } },
+    })
+    if (kind !== 'missing-lock') writeFileSync(join(paths.profile, 'pnpm-lock.yaml'), lockText)
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, { ...release(), productVersion: '3.0.3' })
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await expect(manager.applyRelease(seed, '3.0.3', hooks())).rejects.toThrow(/精确版本/)
+    expect(readFileSync(join(paths.profile, 'package.json'), 'utf8')).toBe(manifestText)
+    if (kind !== 'missing-lock') expect(readFileSync(join(paths.profile, 'pnpm-lock.yaml'), 'utf8')).toBe(lockText)
+    expect(existsSync(join(paths.profile, 'desktop-release.json'))).toBe(false)
+  })
+
+  it('updates the managed plugin without changing DSH, retains settings and third-party plugins, and respects removal', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    const owned = '@agentrouter-top/dsh-codex'
+    const seed = (productVersion: string, pluginVersion: string) => {
+      const dir = join(root, productVersion)
+      createTestSeedMetadata(dir, { ...release('0.1.5-rc.2'), productVersion,
+        managedPlugins: [{ name: owned, version: pluginVersion }] })
+      archiveStore(dir); writeIntegrity(dir)
+      return dir
+    }
+    await manager.applyRelease(seed('1.0.0', '0.6.1'), '1.0.0', hooks())
+    await manager.mutate({ type: 'plugin-add', spec: '@fixture/ordinary@2.0.0' }, hooks())
+    writeFileSync(join(paths.profile, 'cordis.patch.yml'), '# user configuration\n[]\n')
+    await manager.applyRelease(seed('1.0.1', '0.7.0'), '1.0.1', hooks())
+    expect(manager.dshVersion()).toBe('0.1.5-rc.2')
+    expect(manager.releaseVersion()).toBe('1.0.1')
+    expect(manager.listPlugins()).toEqual([
+      { name: owned, version: '0.7.0', updateOwner: 'product' },
+      { name: '@fixture/ordinary', version: '2.0.0' },
+    ])
+    expect(readFileSync(join(paths.profile, 'cordis.patch.yml'), 'utf8')).toBe('# user configuration\n[]\n')
+    await expect(manager.mutate({ type: 'plugin-update', name: owned, version: '99.0.0' }, hooks())).rejects.toThrow(/客户端更新/)
+    await manager.mutate({ type: 'plugin-remove', name: owned }, hooks())
+    await manager.applyRelease(seed('1.0.2', '0.7.1'), '1.0.2', hooks())
+    expect(manager.listPlugins()).toEqual([{ name: '@fixture/ordinary', version: '2.0.0' }])
+    expect(readFileSync(join(paths.profile, 'cordis.patch.yml'), 'utf8')).toContain('user configuration')
+  })
+
+  it('keeps the previous product and plugin when the new seed fails health checks', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    for (const [productVersion, pluginVersion] of [['1.0.0', '0.6.1'], ['1.0.1', '0.7.0']]) {
+      const dir = join(root, productVersion)
+      createTestSeedMetadata(dir, { ...release(), productVersion,
+        managedPlugins: [{ name: '@agentrouter-top/dsh-codex', version: pluginVersion! }] })
+      archiveStore(dir); writeIntegrity(dir)
+    }
+    await manager.applyRelease(join(root, '1.0.0'), '1.0.0', hooks())
+    await expect(manager.applyRelease(join(root, '1.0.1'), '1.0.1', hooks({ healthCheck: async () => { throw new Error('fixture failure') } }))).rejects.toThrow('fixture failure')
+    expect(manager.releaseVersion()).toBe('1.0.0')
+    expect(manager.listPlugins()[0]?.version).toBe('0.6.1')
+  })
+
   it('accepts registry package specs but rejects alternate sources and flags', () => {
     expect(packageNameFromSpec('@scope/plugin@1.2.3')).toBe('@scope/plugin')
     expect(packageNameFromSpec('plugin@next')).toBe('plugin')
@@ -193,6 +379,106 @@ describe('desktop package policy', () => {
 })
 
 describe('desktop project transactions', () => {
+  it('repairs store drift at the same release once, preserving user configuration and plugin removals', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const seed = join(root, 'seed')
+    const owned = '@agentrouter-top/dsh-codex'
+    createTestSeedMetadata(seed, { ...release(), productVersion: '3.0.1', managedPlugins: [{ name: owned, version: '0.7.1' }] })
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await manager.applyRelease(seed, '3.0.1', hooks())
+    await manager.mutate({ type: 'plugin-add', spec: '@fixture/ordinary@2.0.0' }, hooks())
+    await manager.mutate({ type: 'plugin-remove', name: owned }, hooks())
+    const patch = '# user configuration\n[]\n'
+    writeFileSync(join(paths.profile, 'cordis.patch.yml'), patch)
+    const metadata = join(paths.profile, 'node_modules', '.modules.yaml')
+    writeFileSync(metadata, `storeDir: ${JSON.stringify(join(paths.pnpm.store, 'v10'))}\nnodeLinker: hoisted\npackageManager: pnpm@10.27.0\n`)
+    await expect(manager.applyRelease(seed, '3.0.1', hooks())).resolves.toBe(true)
+    expect(manager.listPlugins()).toEqual([{ name: '@fixture/ordinary', version: '2.0.0' }])
+    expect(readFileSync(join(paths.profile, 'cordis.patch.yml'), 'utf8')).toBe(patch)
+    // A different pnpm patch version using the same actual store needs no migration.
+    const modules = JSON.parse(readFileSync(metadata, 'utf8'))
+    writeFileSync(metadata, JSON.stringify({ ...modules, packageManager: 'pnpm@11.8.0' }))
+    await expect(manager.applyRelease(seed, '3.0.1', hooks())).resolves.toBe(false)
+    expect(JSON.parse(readFileSync(metadata, 'utf8')).packageManager).toBe('pnpm@11.8.0')
+  })
+
+  it('preserves the active graph when store repair fails health checks', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, release())
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await manager.applyRelease(seed, '1.0.0', hooks())
+    const metadata = join(paths.profile, 'node_modules', '.modules.yaml')
+    const original = JSON.stringify({ storeDir: join(root, 'old-store'), nodeLinker: 'hoisted' })
+    writeFileSync(metadata, original)
+    await expect(manager.applyRelease(seed, '1.0.0', hooks({
+      healthCheck: async () => { throw new Error('repair health failed') },
+      beforeActivate: async () => { throw new Error('must not activate') },
+    }))).rejects.toThrow('repair health failed')
+    expect(readFileSync(metadata, 'utf8')).toBe(original)
+    expect(manager.dshVersion()).toBe('1.0.0')
+  })
+
+  it('keeps the active project when activation fails before moving it', async () => {
+    const root = temporaryRoot()
+    const paths = resolveDesktopPaths(join(root, 'home'))
+    const seed = join(root, 'seed')
+    createTestSeedMetadata(seed, release())
+    archiveStore(seed); writeIntegrity(seed)
+    const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: writeFakePnpm(root) })
+    await manager.applyRelease(seed, '1.0.0', hooks())
+    const before = readFileSync(join(paths.profile, 'package.json'))
+    await expect(manager.mutate({ type: 'plugin-add', spec: '@fixture/ordinary@2.0.0' }, hooks({
+      beforeActivate: async () => { writeFileSync(paths.rollback, 'unusable rollback destination') },
+    }))).rejects.toThrow('not a directory')
+    expect(readFileSync(join(paths.profile, 'package.json')).equals(before)).toBe(true)
+    expect(manager.listPlugins()).toEqual([])
+  })
+
+  it.each(['ERR_PNPM_NO_OFFLINE_META', 'ERR_PNPM_NO_OFFLINE_TARBALL', 'ERR_PNPM_TARBALL_INTEGRITY'])(
+    'fetches missing user packages only for offline cache misses (%s)', async (code) => {
+      const root = temporaryRoot()
+      const paths = resolveDesktopPaths(join(root, 'home'))
+      const seed = join(root, 'seed')
+      createTestSeedMetadata(seed, release())
+      archiveStore(seed); writeIntegrity(seed)
+      const delegate = writeFakePnpm(root)
+      const manager = new DesktopProjectManager(paths, { node: process.execPath, pnpm: delegate })
+      await manager.applyRelease(seed, '1.0.0', hooks())
+      const metadata = join(paths.profile, 'node_modules', '.modules.yaml')
+      writeFileSync(metadata, JSON.stringify({ storeDir: join(root, 'old-store'), nodeLinker: 'hoisted' }))
+      const before = readFileSync(metadata)
+      const wrapper = join(root, 'cache-miss.mjs')
+      const attempts = join(root, 'attempts.jsonl')
+      writeFileSync(wrapper, `
+import { appendFileSync } from 'node:fs'
+const args = process.argv.slice(2)
+if (!args.includes('store')) {
+  appendFileSync(${JSON.stringify(attempts)}, JSON.stringify(args) + '\\n')
+  console.error(args.includes('--offline') ? ${JSON.stringify(code)} : 'ERR_PNPM_FETCH_FAILED')
+  process.exit(1)
+}
+await import(${JSON.stringify(pathToFileURL(delegate).href)})
+`)
+      const repair = new DesktopProjectManager(paths, { node: process.execPath, pnpm: wrapper })
+      const offlineMiss = code.includes('NO_OFFLINE')
+      await expect(repair.applyRelease(seed, '1.0.0', hooks())).rejects.toThrow(offlineMiss ? 'ERR_PNPM_FETCH_FAILED' : code)
+      const invocations = readFileSync(attempts, 'utf8').trim().split('\n').map(line => JSON.parse(line) as string[])
+      expect(invocations).toHaveLength(offlineMiss ? 2 : 1)
+      if (offlineMiss) {
+        expect(invocations[1]).toContain('--prefer-offline')
+        expect(invocations[1]).toContain('fetch')
+        expect(invocations[1]).toContain('--ignore-scripts')
+        expect(invocations[1]).toContain('--fetch-retries=1')
+      }
+      expect(readFileSync(metadata).equals(before)).toBe(true)
+    },
+  )
+
   it('installs the offline seed and reconciles a mismatched private Host', async () => {
     const root = temporaryRoot()
     const seed = join(root, 'seed')
@@ -362,10 +648,11 @@ describe('desktop project transactions', () => {
     expect(readFileSync(join(paths.profile, 'pnpm-workspace.yaml'), 'utf8'))
       .toContain(`${JSON.stringify('@deepseek-ai/dsh')}: ${JSON.stringify(coreSpec)}`)
     expect(manifest.dependencies['@scope/plugin']).toBe('2.0.0')
-    const invocation = JSON.parse(readFileSync(log, 'utf8')) as { args: string[]; env: Record<string, string> }
-    expect(invocation.args).toContain('add')
-    expect(invocation.args).toContain('@scope/plugin@2.0.0')
-    expect(invocation.args).toContain('--config.registry=https://registry.npmjs.org/')
+    const invocation = JSON.parse(readFileSync(log, 'utf8')) as { args: string[]; history: string[][]; env: Record<string, string> }
+    const addition = invocation.history.find(args => args.includes('add'))
+    expect(addition).toContain('@scope/plugin@2.0.0')
+    expect(addition).toContain('--config.registry=https://registry.npmjs.org/')
+    expect(invocation.args).toContain('--frozen-lockfile')
     expect(invocation.env.NPM_CONFIG_REGISTRY).toBe('https://registry.npmjs.org/')
   })
 
diff --git a/apps/desktop/tests/update-coordinator.spec.ts b/apps/desktop/tests/update-coordinator.spec.ts
index 272ef03..bd0450f 100644
--- a/apps/desktop/tests/update-coordinator.spec.ts
+++ b/apps/desktop/tests/update-coordinator.spec.ts
@@ -42,6 +42,77 @@ describe('desktop release metadata', () => {
 })
 
 describe('desktop update coordinator', () => {
+  it('downloads separately, reports actual progress and waits for an explicit restart', async () => {
+    const events = new Map<string, (value: unknown) => void>()
+    const downloaded = Promise.withResolvers<string[]>()
+    const beforeRestart = vi.fn(async () => {})
+    const updater = { autoDownload: true, autoInstallOnAppQuit: true,
+      on: vi.fn((event, fn) => { events.set(event, fn) }),
+      checkForUpdates: vi.fn(async () => ({ isUpdateAvailable: true, updateInfo: { version: '1.0.1' } })),
+      downloadUpdate: vi.fn(() => downloaded.promise), quitAndInstall: vi.fn() } as unknown as AppUpdater
+    const coordinator = new DesktopUpdateCoordinator(state => state, beforeRestart, updater, () => true)
+    await coordinator.check()
+    await expect(coordinator.download('1.0.2')).rejects.toThrow(/changed/)
+    await expect(coordinator.restart('1.0.1')).rejects.toThrow(/download/)
+    const operation = coordinator.download('1.0.1')
+    await Promise.resolve(); await Promise.resolve()
+    events.get('download-progress')!({ transferred: 25, total: 100, percent: 25 })
+    expect(coordinator.status().progress?.percent).toBe(25)
+    expect(coordinator.status().phase).toBe('downloading')
+    downloaded.resolve([]); await operation
+    expect(coordinator.status().canInstall).toBe(true)
+    expect(beforeRestart).not.toHaveBeenCalled()
+    expect(updater.quitAndInstall).not.toHaveBeenCalled()
+    await coordinator.check()
+    expect(updater.checkForUpdates).toHaveBeenCalledOnce()
+    await coordinator.restart('1.0.1')
+    expect(beforeRestart).toHaveBeenCalledOnce()
+    expect(updater.quitAndInstall).toHaveBeenCalledWith(true, true)
+  })
+
+  it('retains a retry action after a failed download without exposing raw errors', async () => {
+    const updater = { checkForUpdates: vi.fn(async () => ({ isUpdateAvailable: true, updateInfo: { version: '1.0.1' } })),
+      downloadUpdate: vi.fn(async () => { throw new Error('secret path') }), quitAndInstall: vi.fn() } as unknown as AppUpdater
+    const coordinator = new DesktopUpdateCoordinator(state => state, async () => {}, updater, () => true)
+    await coordinator.check(); await coordinator.download('1.0.1')
+    expect(coordinator.status().canDownload).toBe(true)
+    expect(coordinator.status().canInstall).toBe(false)
+    expect(JSON.stringify(coordinator.status())).not.toContain('secret')
+    expect(updater.quitAndInstall).not.toHaveBeenCalled()
+  })
+
+  it('revalidates retained bytes before restart and refuses a changed signed cache', async () => {
+    const updater = { checkForUpdates: vi.fn(async () => ({ isUpdateAvailable: true, updateInfo: { version: '1.0.1' } })),
+      downloadUpdate: vi.fn(async () => ['installer.exe']), quitAndInstall: vi.fn() } as unknown as AppUpdater
+    const beforeRestart = vi.fn(async () => {})
+    const coordinator = new DesktopUpdateCoordinator(state => state, beforeRestart, updater, () => true)
+    const afterDownload = vi.fn().mockResolvedValueOnce(undefined).mockRejectedValueOnce(new Error('changed cache'))
+    Object.defineProperty(coordinator, 'signatureCheck', { value: { beforeDownload: vi.fn(), afterDownload } })
+    await coordinator.check(); await coordinator.download('1.0.1'); await coordinator.restart('1.0.1')
+    expect(afterDownload).toHaveBeenCalledTimes(2)
+    expect(beforeRestart).not.toHaveBeenCalled()
+    expect(updater.quitAndInstall).not.toHaveBeenCalled()
+    expect(coordinator.status().canDownload).toBe(true)
+    expect(coordinator.status().canInstall).toBe(false)
+  })
+
+  it('retains the downloaded release when active work defers restart', async () => {
+    let busy = true
+    const updater = { autoDownload: true, autoInstallOnAppQuit: true,
+      checkForUpdates: vi.fn(async () => ({ isUpdateAvailable: true, updateInfo: { version: '1.0.1' } })),
+      downloadUpdate: vi.fn(async () => []), quitAndInstall: vi.fn() } as unknown as AppUpdater
+    const coordinator = new DesktopUpdateCoordinator(state => state, async () => {
+      if (busy) throw new Error('active task')
+    }, updater, () => true)
+    await coordinator.check()
+    expect((await coordinator.install()).phase).toBe('error')
+    expect(updater.quitAndInstall).not.toHaveBeenCalled()
+    busy = false
+    expect((await coordinator.install()).phase).toBe('ready')
+    expect(updater.quitAndInstall).toHaveBeenCalledOnce()
+    expect(updater.downloadUpdate).toHaveBeenCalledOnce()
+  })
+
   it('installs one Electron release and restarts after download', async () => {
     const states: DesktopUpdateState[] = []
     const downloadUpdate = vi.fn(async () => [])
@@ -71,6 +142,6 @@ describe('desktop update coordinator', () => {
     await expect(coordinator.install()).resolves.toEqual({ phase: 'ready', version: '1.1.0' })
     expect(downloadUpdate).toHaveBeenCalledOnce()
     expect(beforeRestart).toHaveBeenCalledOnce()
-    expect(quitAndInstall).toHaveBeenCalledWith(false, true)
+    expect(quitAndInstall).toHaveBeenCalledWith(true, true)
     expect(states.map(state => state.phase)).toEqual(['checking', 'available', 'installing', 'ready'])
   })
